# Legacy implementation

This directory contains the previous backend, tests, scripts, documentation, and
frontend. It is retained as a behavioral reference while the simpler architecture
is implemented in the active `src/` and `tests/` directories.

The code under `old/` is not part of the active application or test suite.
